[build.environment]
HUGO_VERSION = "0.129.0"
TZ = "America/Denver"

[build]
publish = "public"
command = "hugo -s resume --gc --minify"

# Rewrite a path
[[redirects]]
from = "/"
to = "/resume"
status = 200
